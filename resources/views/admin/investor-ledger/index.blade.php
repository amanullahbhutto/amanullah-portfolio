@extends('layouts.admin')
@section('title','Investor Ledger')
@section('page_title','Investor Ledger')
@section('content')
<div class="d-flex flex-column flex-md-row align-items-md-center justify-content-between gap-3 mb-4"><div><span class="eyebrow">Investor Statement</span><h2 class="h4 mb-1">{{ $investor->name }}</h2><p class="text-muted-custom mb-0">Complete capital and profit transaction history.</p></div><a class="btn btn-outline-theme" href="{{ route('admin.investors.index') }}"><i class="bi bi-arrow-left me-1"></i>Back to Investors</a></div>
<div class="row g-4 mb-4">@foreach([['Current Investment',$investor->current_investment,'bi-cash-stack','orange'],['Profit Share',number_format($investor->profit_share_percentage,2).'%', 'bi-percent','blue'],['Total Profit Earned',$investor->total_profit,'bi-graph-up-arrow','green'],['Pending Profit',$investor->pending_profit,'bi-hourglass-split','purple']] as $stat)<div class="col-sm-6 col-xl-3"><article class="admin-card stat-card"><div class="stat-icon {{ $stat[3] }}"><i class="bi {{ $stat[2] }}"></i></div><div><strong>{{ $stat[1] instanceof \Stringable || is_string($stat[1]) && str_contains($stat[1], '%') ? $stat[1] : 'Rs. '.number_format((float)$stat[1],2) }}</strong><span>{{ $stat[0] }}</span></div></article></div>@endforeach</div>
<section class="admin-card"><div class="admin-card-head"><h2>Ledger Transactions</h2></div><div class="table-responsive"><table class="table table-hover"><thead><tr><th>Date</th><th>Type</th><th>Description</th><th class="text-end">Debit</th><th class="text-end">Credit</th><th class="text-end">Running Balance</th></tr></thead><tbody>@forelse($ledger as $row)<tr><td>{{ $row['date']->format('M d, Y') }}</td><td><span class="duration-pill">{{ $row['type'] }}</span></td><td>{{ $row['description'] }}</td><td class="text-end">{{ $row['debit'] ? 'Rs. '.number_format($row['debit'],2) : '-' }}</td><td class="text-end text-success">{{ $row['credit'] ? 'Rs. '.number_format($row['credit'],2) : '-' }}</td><td class="text-end"><strong>Rs. {{ number_format($row['balance'],2) }}</strong></td></tr>@empty<tr><td colspan="6" class="text-center py-5 text-muted-custom">No ledger transactions yet.</td></tr>@endforelse</tbody></table></div></section>
@endsection
